# Meeting Source Reconciliation

The uploaded CSV contains **328** records. It matched **328** records by legacy ID and **0** additional records by slug against the current public detail corpus; **0** CSV records were not found in the current extraction.

The comparison identified **0** name-presentation differences, **290** address-presentation differences, **0** region-label differences, and **4** time-presentation differences. These are source-format comparisons, not automatic errors: the CSV is the structured authoritative record and will be applied to the rebuilt database while the current public detail URL remains its traceable source.

The detailed machine reconciliation is held in the protected source workspace. The published application will retain only the validated, current meeting data and audit evidence.
