# Media and Downloadable Asset Intake

The rebuild accepts public media exports without requiring administrator credentials. The preferred package is a ZIP of the source `uploads` or media-library directory plus `media_manifest.csv`. The manifest should contain `original_url`, `relative_file_path`, `filename`, `mime_type`, `file_size`, `alt_text`, `caption`, `source_page`, and `last_modified`.

The intake workflow is deliberately separated from publication. First, archive the supplied ZIP outside the deployable source tree and calculate a checksum for each file. Next, parse the manifest and normalize public source URLs. Then map each asset to the page or meeting record that references it, flagging missing files, duplicate filenames, unsupported formats, and assets without alt text. Finally, upload approved public assets to managed object storage and replace legacy references only after the page-level review decision is recorded.

Private exports, backups, database dumps, credentials, session cookies, server configuration, and administrator edit URLs must not enter the public asset library. PDFs and downloadable literature should remain linked through verified managed storage URLs; raw local files must not be placed inside the web project deployment directory.

The current source package contains WordPress attachment metadata but not a complete media-library ZIP. Consequently, the attachment register is complete as metadata, while binary asset transfer and page-by-page asset verification remain pending until a public media archive is supplied.

## Current execution

The reusable intake runner has been executed against the uploaded WordPress attachment metadata. It inventoried **154 attachment records** with no duplicate filenames or missing manifest entries in metadata-only mode. Because no binary media-library ZIP or `media_manifest.csv` was supplied, the workflow has not performed archive extraction, checksum comparison, managed-storage upload, or binary-to-page linkage verification. Those steps remain explicitly pending until the public media archive is uploaded.

## Uploaded archive execution

The uploaded `uploads.zip` was inspected without executing any files. It contained 2,449 files in 170 directories. The filter excluded 214 non-public plugin, cache, security-log, PHP, quota, and runtime artifacts, with zero unsafe traversal paths. The approved subset contained 2,235 public-looking files; attachment-level reconciliation linked all 154 WordPress attachment records to 160 archive variants. A canonical public subset of 154 attachment files was then prepared outside the project, uploaded to managed storage, and verified with **154 successful uploads and zero missing upload results**. The final linkage is recorded in `final_uploaded_media_manifest.csv` and `final_uploaded_media_report.json`.

The archive also contained 426 duplicate-content groups and 2,075 approved-looking derivative or otherwise unlinked files. These were not blindly published; the canonical attachment subset is the controlled public asset set, while the remaining archive files remain excluded or review-only.

## Corrected linkage and upload execution

The first canonical preparation was corrected because basename-only copying could collapse files with identical names across regional sites. The corrected workflow now preserves a unique relative path containing the source export and archive subdirectory. All **154 attachment records** have distinct archive paths, all **154** have a source-page relationship from the WordPress XML exports, and all **154** unique files uploaded successfully to managed storage with zero missing upload results. The corrected per-attachment manifest is `final_uploaded_media_manifest.csv`; it includes the source export, archive path, source-page JSON, unique relative path, managed-storage URL, and upload status.

The managed-storage URL join now uses the full unique relative path rather than the filename alone, so files such as `logo.png` from separate regional exports retain distinct URLs. Every final-manifest row includes its SHA-256, basename collision count, identical-binary count, archive variant count, derivative variant count, matched archive-variant paths, canonical-selection status, duplicate or variant rationale, binary-duplicate rationale, derivative-variant rationale, and unresolved status. The derivative count is calculated only from that attachment’s `archive_files` set in `media_attachment_linkage.json`; it does not use global filename-family matching. The final controlled upload set retains the exact canonical attachment, while linked alternatives remain traceable for review. Of the 154 records, 91 share a basename with another source-relative attachment and 96 have an identical binary elsewhere in the canonical set; these are intentionally retained as independently traceable source attachments. The evidence report records **zero unresolved rows**, **154 successful uploads**, **154 source-page links**, and **zero unlinked canonical archive paths**.
