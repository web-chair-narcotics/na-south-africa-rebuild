# Uploaded Meeting Source Parse

The uploaded CSV contains **328** rows across **7** listed days and **4** source regions. **328** rows include an address and **50** include a conference URL.

Regions: Johannesburg, Kwazulu-Natal, Pretoria, Western Cape.

The full normalized CSV archive and text-extracted 16-page directory are retained in the protected source workspace for reconciliation. The PDF text is used to confirm the printable directory ordering and does not overwrite structured CSV fields by itself.
