# Uploaded Regional Source Inventory

The package contains **5 WordPress exports**, **7 CSV snapshots**, and **4 JSON text exports**.

Across the WordPress exports there are **1,710 items**, including **84 pages**, **154 attachments**, **473 TSML locations**, and **669 TSML meeting records**.

The regional meeting snapshots are retained with their source file names and timestamps. The source inventory excludes private edit URLs from any public migration output; those fields remain source-only and will not be copied into the application.

## Reconciliation additions

The 669 WordPress TSML meeting items, 1,278 CSV snapshot rows, and 294 JSON records resolve to 620 distinct meeting IDs. Forty-nine IDs required explicit precedence decisions; each was resolved using active status, address/coordinate completeness, and latest update timestamp, while preserving the decision as source evidence.

The printable day-and-region directory produced 252 listing lines. All 252 now have a structured CSV name match at the documented similarity threshold. The CSV remains the authoritative field source; the PDF confirms public grouping, day/time presentation, and venue-line coverage.

The regional navigation exports contain 107 raw menu items, resolving to 100 distinct title/target pairs. The public URL register contains 1,267 unique content, meeting, location, and event URLs. Meeting and location URLs are routed to the structured finder; information and event URLs remain explicit content-migration candidates.

A total of 81 public regional WordPress page records have been staged in the rebuilt content model as drafts. No staged page was automatically published. Each draft retains its source export and legacy URL in review metadata.

## Final route and decision application

All 100 distinct navigation pairs now have concrete decisions. The 1,267 unique legacy URLs are classified into structured finder routes, page-draft review, news/event routes, content-route review, or technical/archive handling. The 83 previously unresolved object references are resolved through post-ID lookup, custom-section mapping, area routing, external-link preservation, or explicit source-route review.

All 49 uploaded meeting conflict decisions were applied to matching rebuilt meeting records as audit events and source notes. A final database check found zero unsafe published in-person or hybrid meetings lacking address, map-pin, spelling, contact, coordinate, or street-address QA evidence.

## Final per-URL routing

The finalized registers now assign all 1,267 regional public URLs and all 100 navigation entries to concrete rebuilt destinations or archive/noindex handling. Routes use the actual rebuilt `/meetings`, `/areas`, `/about`, `/recovery`, `/literature`, `/contact`, and `/news` pages; technical feeds and APIs are explicitly excluded from public page migration. Page content remains draft-staged where editorial publication is pending, but no route remains in an unresolved placeholder bucket.
